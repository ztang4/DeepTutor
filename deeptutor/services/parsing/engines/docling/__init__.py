"""Docling engine — structured document conversion (downloads local models).

Converts every input format advertised by the supported Docling release into
Markdown. Downloads layout/table models on first run, so it shares the same
``allow_local_model_download`` gate as MinerU local.
"""
